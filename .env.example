# Application Settings
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database URL - REQUIRED: PostgreSQL connection string
# For Vercel Postgres: Get this from Vercel Dashboard > Storage > Postgres
# For local PostgreSQL: postgresql://user:password@localhost:5432/database_name
DATABASE_URL=postgresql://user:password@localhost:5432/prettysnail

# JWT Secret - REQUIRED: Generate a strong random secret for production
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Default Admin Credentials (used only on first initialization)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
