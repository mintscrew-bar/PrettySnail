# ===================================================================
# ENVIRONMENT CONFIGURATION - DEVELOPMENT
# ===================================================================
# Copy this file to .env.local for local development
# DO NOT commit .env.local to git - it contains sensitive credentials
#
# ⚠️  SECURITY WARNING:
# - Change ALL values before deploying to production
# - Never use default values in production
# - Keep .env.local in .gitignore
# ===================================================================

# Application Settings
# REQUIRED in production - must be your actual domain (not localhost)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database URL - REQUIRED
# For Vercel Postgres: Get this from Vercel Dashboard > Storage > Postgres
# For local PostgreSQL: postgresql://user:password@localhost:5432/database_name
# ⚠️  WARNING: This contains sensitive credentials - never commit to git
DATABASE_URL=postgresql://user:password@localhost:5432/prettysnail

# JWT Secret - REQUIRED
# ⚠️  CRITICAL: Generate a strong random secret for production
# Must be at least 32 characters long
# Generate with: openssl rand -base64 32
# NEVER use the default value in production!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Default Admin Credentials - REQUIRED
# These are used ONLY on first initialization when no admin exists
# ⚠️  WARNING: Change these values immediately!
# - Username: Choose a unique username (not "admin")
# - Password: Use a strong password (min 8 chars, recommended 16+)
# Default values shown below are for LOCAL DEVELOPMENT ONLY
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
